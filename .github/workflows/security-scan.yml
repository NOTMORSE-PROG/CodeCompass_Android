# Consolidated into ci.yml — this file is no longer active.
name: Security Scan (retired)
on:
  workflow_dispatch:
jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow has been consolidated into ci.yml"

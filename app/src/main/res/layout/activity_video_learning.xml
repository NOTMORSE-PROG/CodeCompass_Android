<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:background="@color/colorBackground">

        <!-- Toolbar -->
        <androidx.appcompat.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="@color/colorSurface"
            app:navigationIcon="@drawable/ic_arrow_back"
            app:titleTextColor="@color/colorTextPrimary" />

        <!-- Video player — height set to screenWidth × 9/16 in code -->
        <WebView
            android:id="@+id/webViewVideo"
            android:layout_width="match_parent"
            android:layout_height="200dp"
            android:background="#000000" />

        <!-- Watch-progress row -->
        <LinearLayout
            android:id="@+id/layoutWatchProgress"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:background="@color/colorSurface"
            android:paddingHorizontal="12dp"
            android:paddingVertical="8dp">

            <ProgressBar
                android:id="@+id/progressWatch"
                style="@style/Widget.AppCompat.ProgressBar.Horizontal"
                android:layout_width="0dp"
                android:layout_height="6dp"
                android:layout_weight="1"
                android:progressDrawable="@drawable/bg_progress_bar_yellow"
                android:max="100"
                android:progress="0"
                android:layout_marginEnd="8dp" />

            <TextView
                android:id="@+id/tvWatchPercent"
                android:layout_width="72dp"
                android:layout_height="wrap_content"
                android:text="0:00 / 5:00"
                android:textSize="12sp"
                android:textColor="@color/colorTextSecondary" />

        </LinearLayout>

        <!-- Scrollable content: video info + quiz -->
        <ScrollView
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:fillViewport="true">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:padding="16dp">

                <!-- Video title -->
                <TextView
                    android:id="@+id/tvVideoTitle"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text=""
                    android:textSize="15sp"
                    android:textStyle="bold"
                    android:textColor="@color/colorTextPrimary"
                    android:layout_marginBottom="4dp" />

                <!-- Channel name -->
                <TextView
                    android:id="@+id/tvVideoChannel"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text=""
                    android:textSize="13sp"
                    android:textColor="@color/colorTextSecondary"
                    android:layout_marginBottom="16dp"
                    android:visibility="gone" />

                <View
                    android:layout_width="match_parent"
                    android:layout_height="1dp"
                    android:background="@color/dividerGray"
                    android:layout_marginBottom="20dp" />

                <!-- ── Quiz section ───────────────────────────────────────────── -->

                <!-- Locked: watch 5 minutes first -->
                <LinearLayout
                    android:id="@+id/layoutQuizLocked"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="vertical"
                    android:gravity="center"
                    android:paddingVertical="24dp">

                    <TextView
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="🔒"
                        android:textSize="36sp"
                        android:layout_marginBottom="12dp" />

                    <TextView
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="@string/video_watch_to_unlock"
                        android:textSize="14sp"
                        android:textColor="@color/colorTextSecondary"
                        android:gravity="center"
                        android:lineSpacingMultiplier="1.4" />

                </LinearLayout>

                <!-- Take Quiz button — shown after 60 % is reached, hidden until then -->
                <Button
                    android:id="@+id/btnTakeQuiz"
                    style="@style/Widget.MaterialComponents.Button"
                    android:layout_width="match_parent"
                    android:layout_height="52dp"
                    android:text="@string/quiz_take"
                    android:textColor="@color/colorOnPrimary"
                    android:backgroundTint="@color/colorPrimary"
                    android:visibility="gone" />

            </LinearLayout>
        </ScrollView>

    </LinearLayout>

    <!-- Fullscreen overlay — GONE by default, shown when user taps fullscreen button -->
    <FrameLayout
        android:id="@+id/fullscreenContainer"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#000000"
        android:visibility="gone" />

</FrameLayout>

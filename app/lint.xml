<?xml version="1.0" encoding="UTF-8"?>
<lint>
    <!--
        Retrofit 3.x and OkHttp 5.x are major version bumps with breaking API changes.
        Updating them requires significant code migration and testing.
    -->
    <issue id="NewerVersionAvailable" severity="ignore" />

    <!--
        TrustAllX509TrustManager warnings originate from the BouncyCastle dependency
        (bcpkix-jdk15to18), a third-party library we do not control.
    -->
    <issue id="TrustAllX509TrustManager" severity="ignore" />

    <!--
        IconXmlAndPng: The adaptive icon setup (mipmap-anydpi XML + density-specific webp
        bitmaps) is the standard Android pattern for launcher icons. This is expected.
    -->
    <issue id="IconXmlAndPng" severity="ignore" />
</lint>
